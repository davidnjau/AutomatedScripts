# CLAUDE.md — entries

This directory contains a separate codebase from `assign/`.

## Strict Rules

- **Never read, edit, move, or delete any file inside `assign/`** when working in this directory.
- All work is scoped exclusively to `entries/` and the root-level shared files (e.g. `.gitignore`, root `CLAUDE.md`).
- Do not import, copy, or reference code from `assign/` unless explicitly instructed.
